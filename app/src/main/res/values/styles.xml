<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Splash Screen -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/launch_background_splash</item>
    </style>

    <!-- Toolbar -->
    <style name="Toolbar">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <!-- Toolbar Titulo-->
    <!--TODO: 21-01-19 Evitar blur en splash-->
    <style name="ToolbarTitle">
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/colorTextTitle</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--Boton perfil de alumno -->
    <style name="ButtonPerfil">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:clickable">false</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/colorTextButton</item>
        <item name="android:textSize">@dimen/font_small</item>
    </style>

    <!--Boton cerrar sesion -->
    <style name="ButtonLogout">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/colorTextButton</item>
        <item name="android:textSize">@dimen/font_small</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--Logo universidad-->
    <style name="ImageButtonLogo">
        <item name="android:adjustViewBounds">true</item>
        <item name="android:background">@drawable/all_ripple_effect</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <!--Banner recyclerview-->
    <style name="TextViewInformacion">
        <item name="android:background">@color/colorSecondary</item>
        <item name="android:textSize">@dimen/font_large</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">20dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--Spinner tesistas-->
    <style name="SpinnerTesistas">
        <item name="android:background">@color/colorTory</item>
        <item name="android:spinnerMode">dialog</item>
    </style>

    <!--Boton tesistas-->
    <style name="ButtonTesistas">
        <item name="android:background">@color/colorTory</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
        <item name="android:textColor">@color/colorTextButton</item>
    </style>
</resources>
